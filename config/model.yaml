model:
  name: random_forest

  params:
    n_estimators: 200
    max_depth: 10
    min_samples_split: 5
    min_samples_leaf: 2
    max_features: sqrt
    random_state: 42
    n_jobs: -1

# To test XGBoost later, replace the section above with:
#
# model:
#   name: xgboost
#   params:
#     n_estimators: 200
#     learning_rate: 0.1
#     max_depth: 6
#     subsample: 0.8
#     colsample_bytree: 0.8
#     random_state: 42
#     eval_metric: logloss